# SKILL: audit
INPUT: target store (apple or google) and the project path.
OUTPUT: a report in REPORT.md format saved to /reports.
RULES: read code paths, never assume from names; every finding has evidence; never change the project during an audit.
