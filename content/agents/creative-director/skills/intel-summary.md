# SKILL: intel-summary
INPUT: files in /intel newer than the last LOG entry.
OUTPUT: hooks seen, formats seen, longest-running ads (if dates were given), and the white space nobody is using. Never claim to have seen what was not pasted.
