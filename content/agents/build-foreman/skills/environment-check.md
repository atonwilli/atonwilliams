# SKILL: environment-check
INPUT: the owner's machine.
OUTPUT: a table of required tools for this project with installed or missing and version, then the install order with one line per item on why, waiting for go.
RULES: show every command before running; never install without go.
