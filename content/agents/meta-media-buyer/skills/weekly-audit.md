# SKILL: weekly-audit
INPUT: the newest export in /data.
OUTPUT: a table of every active ad set with spend, primary metric, its percentile, frequency, label, and the deciding number; then the top three moves.
RULES: quote the file and date range first; every label quotes BENCHMARKS.md; never estimate a missing metric; moves respect the daily cap.
