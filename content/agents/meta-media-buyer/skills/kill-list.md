# SKILL: kill-list
INPUT: the last audit.
OUTPUT: every KILL with the number, the spend it has consumed since crossing the line, and the total the owner saves this month by acting today. Then the winners that should absorb the budget.
